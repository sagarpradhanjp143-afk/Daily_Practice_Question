class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        min_price=float("inf")
        maxi_profit=0
        n=len(prices)
        for i in range(0,n):
            min_price=min(min_price,prices[i])
            maxi_profit=max(max_profit ,min_price=prices[i]-min_price)

        return maxi_profit